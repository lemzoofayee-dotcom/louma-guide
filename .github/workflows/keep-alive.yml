name: Supabase Keep-Alive

# Empêche la mise en pause auto du projet Supabase (tier gratuit = pause après 7 jours d'inactivité).
# Ping léger de la base TOUS LES JOURS → grosse marge même si GitHub décale/saute un run.
# (Avant : 2x/sem lundi+jeudi → marge trop courte, a laissé pauser le 21/06/2026.)

on:
  schedule:
    - cron: "0 6 * * *" # tous les jours, 06:00 UTC (~8h Paris)
  workflow_dispatch: {} # permet de lancer manuellement depuis l'onglet Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST
        run: |
          curl -sS -f \
            "${{ secrets.SUPABASE_URL }}/rest/v1/dishes?select=id&limit=1" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            && echo "✅ Supabase actif" \
            || (echo "❌ Ping échoué" && exit 1)
